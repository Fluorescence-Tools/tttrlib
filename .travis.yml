dist: xenial
language: python
sudo: required
python:
  - "3.7"

before_install:
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then MINICONDA_OS=Linux; else MINICONDA_OS=MacOSX; fi
  - wget https://repo.anaconda.com/miniconda/Miniconda3-latest-$MINICONDA_OS-x86_64.sh -O miniconda.sh
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  - conda config --set channel_priority strict
  - conda config --add channels conda-forge
  - conda info -a
  - conda create -n build python=3.7 git conda-build
  - source activate build
  - git clone https://github.com/Fluorescence-Tools/conda-recipes.git
  - conda build conda-recipes/tttrlib

install:
  - conda install --use-local tttrlib

script:
  - conda install nose coverage
  - ln -sf $(which nosetests) $(pwd)/test/
  - cd $(pwd)/test/
  - echo $PWD
  - nosetests --with-coverage test_*.py

after_success:
  - pip install codacy-coverage
  - coverage xml
  - python-codacy-coverage -r coverage.xml
  - if not "%APPVEYOR_REPO_BRANCH%" == "master" exit 0
  - conda install anaconda-client
  - anaconda --verbose --token %$CONDA_UPLOAD_TOKEN% upload /home/travis/miniconda/conda-bld/**/tttrlib-*.tar.bz2 --force
