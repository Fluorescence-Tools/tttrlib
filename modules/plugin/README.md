# `plugin` — Dynamic Plugin Host System

Plugin host architecture for loading runtime spectroscopy/decay analysis plugins dynamically.

## Contents

- **`PluginHost.h` / `PluginHost.cpp`**: Dynamic library loader, symbol resolver, and plugin lifecycle manager.

## Dependencies

- Depends on `util`.
