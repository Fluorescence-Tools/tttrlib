# `io/sm` — Single-Molecule (.sm) Format I/O

This module implements reader support for single-molecule `.sm` file format.

## Contents

- **`io_sm.h` / `io_sm.cpp`**: `.sm` header and record decoder.

## Dependencies

- Depends on `io/base`, `util`, nlohmann/json.
