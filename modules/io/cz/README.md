# `io/cz` — Carl Zeiss Format I/O

This module implements reader support for Carl Zeiss ConfoCor3 `.raw` files.

## Contents

- **`io_cz.h` / `io_cz.cpp`**: ConfoCor3 header and record stream parser.

## Dependencies

- Depends on `io` (`io/base`).
