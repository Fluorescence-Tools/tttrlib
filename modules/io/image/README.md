# `io/image` — TIFF / Image I/O

Image array reading and writing routines (TIFF 2D/3D stack formats).

## Contents

- **`TiffArrayIO.h` / `TiffArrayIO.cpp`**: TIFF image stack reader and writer implementation using libtiff.

## Dependencies

- Depends on `util`.
