# Interfaces reachable in Python and missing from another binding.
#
# Format:  <file.i>: <bindings, comma-separated>  # why
#
# A gap here is DECLARED, not approved. Most of these say "unreviewed drift":
# they are what tools/check_binding_parity.py found on 2026-08-11, when the
# four %include lists had diverged by 16-18 files while every non-Python
# master interface carried a comment claiming they were identical. Writing
# them down is what turns an invisible gap into a reviewable one; closing them
# is T-20260811-09.
#
# Deleting a line here without adding the %include makes the check fail, and
# so does leaving a line for a gap that has been closed. Both are on purpose.

documentation.i: java  # DELIBERATE: adding it is a NO-OP, measured 2026-08-11 by generating the java wrapper with and without it -- zero proxies differ and zero javadoc comments appear. It is a file of %feature("docstring") blocks, which is python-oriented; SWIG-Java takes its documentation from %feature("autodoc") (already set in ext/java/tttrlib.i) and ignores docstring features. Closing this gap would need the docs re-expressed as %javadoc, which is a separate piece of work, not an %include.
Deconvolution.i: java  # DELIBERATE: the WHOLE family IS in java, via ext/java/helpers.i -- richardson_lucy_2d_into, richardson_lucy_3d_into (double[][][] via IN_ARRAY3), richardson_lucy_events_2d_into, wiener_deconvolve_2d_into and scan_blur_kernel_1d_into, caller preallocating, results row-major. The interface stays out because its flat entry points return through ARGOUTVIEWM_ARRAY2/3, which java cannot marshal, and its vector-returning overloads take a bare const double* with no length so no IN_ARRAY typemap matches. In r and js.
Jitter.i: java  # DELIBERATE: the capability IS in java, via ext/java/helpers.i -- jitter_coordinates_into(double[][], double[], long) dithers IN PLACE (INPLACE_ARRAY2, so the writes land back in the caller's array), plus events_from_counts_into and counts_from_events_into on the preallocate-and-fill shape. The interface stays out because its flat entry points return through ARGOUTVIEWM_ARRAY2, which java cannot marshal. In r and js.
Sampling.i: java  # DELIBERATE: the capability IS in java, via ext/java/helpers.i -- weighted_choice_into(double[], int[]) and sample_from_cdf_into(double[], double[], double[], boolean), the caller preallocating exactly as for Pda/CLSMImage/TTTR. The interface itself stays out because BOTH its functions return through ARGOUTVIEWM_ARRAY1, which java cannot marshal, so %include-ing it would add two uncallable overloads and no usable surface (tried and reverted 2026-08-11). In r and js, which can return an array.
HmmLattice.i: r,java,js  # mine, added 2026-08-11 and not yet offered to the others; its IN_ARRAY/INPLACE typemaps work there, so this is a %include away.
Kalman.i: java  # DELIBERATE: all three outputs (T x dim, T x dim x dim, T) return through ARGOUTVIEWM_ARRAY1/2/3, which jarrays.i cannot marshal; an `_into` variant on the preallocate-and-fill shape would be the Java surface, none exists yet, so the interface stays out rather than emitting uncallable overloads beside a working path. In r and js, whose argout typemaps cover ranks 1-3.
MaxEntTcspc.i: java  # DELIBERATE: the capability IS in java. The SOLVERS wrap directly (solve_tcspc_mem_lifetime -> MemTcspcResult by value). The design-matrix BUILDERS reach java via ext/java/helpers.i -- tcspc_build_fi_lifetimes_into / _distances_into, which take the four outputs as four INPLACE_ARRAY1 double[] and fill them in ONE native call; the int return is Fi's element count, from which n_data = Fi/tau.length gives the other three. The interface stays out because %include-ing it would add the argout overloads as SWIGTYPE_p_p_double beside the working helpers. In r and js.
